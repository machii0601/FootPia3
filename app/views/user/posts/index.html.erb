<h1>投稿一覧</h1>

<% @posts.each do |post| %>
  <p>タイトル</p>
  <span><%= post.title %></span>
  <p>本文</p>
  <span><%= post.post_content %></span>
<% end %>
<%= link_to "新規登録", new_post_path, class: 'btn btn-primary' %>